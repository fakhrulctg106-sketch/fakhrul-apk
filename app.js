const stored=JSON.parse(localStorage.getItem('fakhrulapk_apps')||'[]'); const apps=[...stored,...DEFAULT_APPS];
let category='All';
function esc(s){return String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]))}
function card(a){return `<article class="app-card"><img src="${esc(a.icon||'assets/icon-default.svg')}" alt=""><div class="app-info"><span class="tag">${esc(a.category)}</span><h3>${esc(a.name)}</h3><p>${esc(a.developer)}</p><div class="meta">${esc(a.version)} · ${esc(a.size)}</div></div><a class="download" href="app.html?id=${encodeURIComponent(a.id)}">View</a></article>`}
function render(){const q=(document.getElementById('search')?.value||'').toLowerCase(); const list=apps.filter(a=>(category==='All'||a.category===category)&&(`${a.name} ${a.developer} ${a.category}`).toLowerCase().includes(q)); const el=document.getElementById('apps'); if(el){el.innerHTML=list.map(card).join('')||'<div class="empty">No apps found.</div>';document.getElementById('count').textContent=`${list.length} apps`}}
function filterApps(){render()} function setCategory(c){category=c;render()}
function detail(){const id=new URLSearchParams(location.search).get('id'); const a=apps.find(x=>x.id===id); const el=document.getElementById('detail'); if(!el)return; if(!a){el.innerHTML='<div class="empty">App not found.</div>';return} el.innerHTML=`<div class="detail-card"><img class="detail-icon" src="${esc(a.icon)}" alt=""><div><span class="tag">${esc(a.category)}</span><h1>${esc(a.name)}</h1><p class="developer">By ${esc(a.developer)}</p><p>${esc(a.description)}</p><div class="facts"><span><b>Version</b>${esc(a.version)}</span><span><b>Size</b>${esc(a.size)}</span><span><b>Android</b>${esc(a.android)}</span></div><a class="primary download-big" href="${esc(a.apk)}" ${a.apk==='#'?'onclick="alert(\'Add your real APK URL from the Admin page.\');return false;"':''}>Download APK</a></div></div>`}
render(); detail();